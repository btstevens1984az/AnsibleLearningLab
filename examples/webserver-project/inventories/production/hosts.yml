---
all:
  children:
    production:
      children:
        webservers:
          hosts:
            web01.example.com:
              ansible_host: 10.0.1.10
            web02.example.com:
              ansible_host: 10.0.1.11
        dbservers:
          hosts:
            db01.example.com:
              ansible_host: 10.0.2.10
      vars:
        ansible_user: ansible
        env: production
