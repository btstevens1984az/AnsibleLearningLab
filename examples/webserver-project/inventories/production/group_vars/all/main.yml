---
# Variables applied to every host in this inventory
ntp_servers:
  - 0.pool.ntp.org
  - 1.pool.ntp.org

timezone: UTC

# Reference vault vars (encrypt with ansible-vault in real deployments)
# db_password: "{{ vault_db_password }}"
