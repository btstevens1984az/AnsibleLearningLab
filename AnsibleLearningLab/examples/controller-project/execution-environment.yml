---
version: 3
images:
  base_image:
    name: quay.io/centos/centos:stream9
dependencies:
  galaxy: requirements.yml
  python: requirements.txt
additional_build_steps:
  prepend_final: |
    RUN dnf install -y openssh-clients rsync && dnf clean all
